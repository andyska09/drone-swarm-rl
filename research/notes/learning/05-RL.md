run                                    one python run/train.py -> runs/<name>/
│
└─ update              ×num_updates    carry in -> carry out, one CSV row
   │
   ├─ rollout          ×num_steps      COLLECT — weights frozen
   │  │                                one iteration = one decision, taken by
   │  │                                all num_envs drones at the same time
   │  │
   │  │  ┌─────────────────────────────────────────────────────┐
   │  │  │  everything in here is width num_envs               │
   │  ├──│  net(obs)          (num_envs, obs) -> mean, std, V  │  batched matmul
   │  ├──│  sample action     (num_envs, 4)  thrust + 3 rates  │
   │  ├──│  env.step          vmap over num_envs               │  the only vmap
   │  │  │    └─ sim step ×steps_per_action   RK4 at sim_dt    │
   │  └──│  store transition  obs, action, logp, V, r, done    │
   │     └─────────────────────────────────────────────────────┘
   │
   │                                   out: a box of (num_steps, num_envs, ...)
   │
   ├─ bootstrap + GAE                  after the rollout, before learning
   │                                   V(final obs) for timeouts, then advantages
   │
   └─ learning phase                   LEARN — simulator idle, box only
      │
      └─ epoch         ×update_epochs  one full pass over the box
         │                             shuffle the box, cut into num_minibatches piles
         │
         └─ minibatch  ×num_minibatches
            ├─ loss                    clipped policy + value + entropy
            └─ adam step               ← the only place weights move
   ▲
   └── box discarded; drones keep flying from where they were

num_updates    = total_timesteps / (num_steps × num_envs)
minibatch_size = num_steps × num_envs / num_minibatches


THE LOSS — one minibatch, swarm/learn/ppo.py:229

    loss = policy_loss  +  vf_coef × value_loss  −  ent_coef × entropy
           ~1              ~1 after reward normalization      0 for now

policy_loss    make good actions more likely, but do not move far
               ratio = exp(new logp − old logp)     1.0 = policy unchanged
               A     = advantage, standardized to mean 0 spread 1
               loss  = −mean( min( ratio × A,  clip(ratio, 1±clip_eps) × A ) )
               the clip is why the same box can be reused update_epochs times

value_loss     teach the critic to guess the return
               loss = ½ mean( (V − target)² )       target = advantage + old V
               squares the error, so the reward scale lands here — this is the
               term reward normalization exists to keep near 1

entropy        keep the policy from going deterministic too early
               entropy of a Gaussian depends on log_std only, not on the state
               ent_coef = 0.0, so this term is off

Every mean is masked: dead drones do not contribute. _mean(x, mask).


